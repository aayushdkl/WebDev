// The difference between useState(()=>{}) and useState(function_name()) is that the former renders the function only once, while the latter renders the function every time the component is rendered.
